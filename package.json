{
  "name": "ghostprotocol",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "build": "bun run --cwd frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
